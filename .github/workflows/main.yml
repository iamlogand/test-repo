name: DummyWorkflow

on:
  workflow_dispatch:
  pull_request:

jobs:
  Test:
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: Create file
        shell: PowerShell
        run: |
          New-Item -Path . -Name "dummyFile.txt" -ItemType "file" -Value "ABC"
